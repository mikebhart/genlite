{% extends 'base.twig' %}

{% block content %}

    <article id="product-{{ post.id }}">
       
        <div class="container">
            <div class="row">

                <div class="col-12">

                    {# {{ dump(product) }} #}

                    {# {{ dump(post.meta()) }} #}

                    <h1>{{ post.title }}</h1>
                    {{ post.content }}
                   <img style="height: 100px; width: 100px;" src="{{ post.thumbnail.src }}"/>

                    <p>{{ post.meta('_price') }}</p>




                </div>
            </div>
        </div>

    </article>

{% endblock %}
